{
  "name": "lru-cache",
  "version": "1.1.2",
  "description": "LRU Cache.",
  "main": "./src/index.js",
  "files": [
    "/src"
  ],
  "repository": "github:olnazx/lru-cache",
  "license": "Zlib",
  "devDependencies": {
    "jest": "^26.4.2"
  },
  "scripts": {
    "test": "jest --env=node"
  }
}
