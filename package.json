{
  "name": "lru-cache",
  "version": "1.0.0",
  "description": "LRU Cache.",
  "main": "./src/index.js",
  "files": [
    "/src"
  ],
  "repository": "gitlab:olnazx/lru-cache",
  "license": "Zlib",
  "devDependencies": {
    "jest": "^25.1.0"
  },
  "scripts": {
    "test": "jest --env=node"
  }
}
